apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: processor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames: ["app"]
  selector:
    matchLabels:
      app: processor
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scheme: http
