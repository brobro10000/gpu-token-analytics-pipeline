apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: platform
  labels:
    app: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/brobro10000/worker:latest
          imagePullPolicy: Always
          env:
            - name: KAFKA_BOOTSTRAP
              value: "kafka.platform.svc.cluster.local:9092"

            - name: KAFKA_TOPIC
              value: "gpu-metadata"

            - name: MONGO_URI
              value: "mongodb://mongo.platform.svc.cluster.local:27017"

            - name: MONGO_DB
              value: "ca4"

            - name: MONGO_COLLECTION
              value: "gpu_metadata"

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

      restartPolicy: Always
